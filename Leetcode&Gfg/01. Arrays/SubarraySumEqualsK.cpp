// Leetcode: 560. Subarray Sum Equals K   --->   Given an array of integers nums and an integer k, return the total
// number of subarrays whose sum equals to k.
// A subarray is a contiguous non-empty sequence of elements within an array.

// Example 1:
// Input: nums = [1,1,1], k = 2
// Output: 2

// Example 2:
// Input: nums = [1,2,3], k = 3
// Output: 2

// Algortihm: A subarray is a contiguous part of the array. Example:
//            For the arr: [1, 2, 3, 4, 5, 6]
//            Subarrays can be: [1] or [2] or [1, 2] or [1, 2, 3] or [2, 3] or [4, 5, 6], etc.
//            But [1, 3, 4] or [1, 3] or [2, 3, 5] cannot be called a subarray.


#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    int subarraySum(vector<int>& nums, int k) {
        
    }
};

int main() {
    vector<int> nums = {1, 1, 1};
    int k = 2;



}